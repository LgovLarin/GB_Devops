6. Запустить в одном терминале программу, а в другом терминале посмотреть PID процесса и остановить с помощью kill, посылая разные типы сигналов. Что происходит?
a)
kill -1 15381 // Обрыв терминальной линии
b)
Завершил cat выполнив kill -2 15685
ivan@ivan-host:~$ cat > file1
wdwd
ivan@ivan-host:~$ wdwd
c)
kill -8 15713 // Исключение в операции с плавающей точкой (стек памяти сброшен на диск)
d)
kill -9 15741  // Убито
e)
kill -11 15763 // Ошибка сегментирования (стек памяти сброшен на диск)
f)
kill -15 15782 // Завершено

Все 64 пробовать не стал пока, чтобы не раздувать ДЗ)
